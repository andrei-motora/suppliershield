{
  "name": "suppliershield",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install && pip install -r requirements.txt"
  }
}
